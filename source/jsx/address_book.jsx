var React = require('react');
var Contact = require('./../js/contact.js');
var ContactDesc = require('./contactDesc.jsx');
var contactDetail = require('./contactDetail.jsx');

var AddressBook = React.createClass({
    render: function () {
        var contactHTML = [];

    }
});

module.export = AddressBook;